# AWS Advanced JDBC Driver Demo - Environment Configuration
# Copy this file to .env and update the values as needed
# All variables are optional - CDK will create defaults if not provided

# Aurora cluster name (default: demo-app)
# AURORA_CLUSTER_ID=demo-app

# Database username (default: postgres)
# AURORA_DB_USERNAME=postgres

# Database name (default: postgres)
# AURORA_DB_NAME=postgres

# AWS region (default: AWS CLI configured region)
# AWS_REGION=us-east-1

# Existing VPC ID (default: CDK creates new VPC)
# AWS_VPC_ID=vpc-xxxxxxxxx

# Existing security group (default: CDK creates new SG with port 5432 open)
# AWS_SECURITY_GROUP_ID=sg-xxxxxxxxx

# Existing subnet group (default: CDK creates new subnet group)
# AWS_DB_SUBNET_GROUP_NAME=existing-subnet-group

# Database password (set automatically by setup script)
# DB_PASSWORD=your_database_password_here

# =============================================================================
# Usage Instructions
# =============================================================================

# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Update the values in .env as needed:
#    - For most users, you can leave most values empty and let CDK create new resources
#    - If you have existing VPC/Security Group/Subnet Group, specify their IDs/names
#    - The DB_PASSWORD will be set automatically by the setup script
#
# 3. Run the CDK setup script:
#    ./setup-aurora-cdk.sh
#
# 4. Run the demo:
#    ./demo.sh standard-jdbc
#    ./demo.sh aws-jdbc-wrapper
#    ./demo.sh read-write-splitting
